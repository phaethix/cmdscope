# npx-install

## Simple string guard
- Sees two package-ish strings; whether the registry is contacted is unclear.
- Runmark marks the package install and possible registry contact.

## RunmarkFacts incremental
- `external_network=true`; install evidence on `npx`; touches stay empty.

## Notes
- Handwritten baseline for Spike differentiation.
- Not a verified claim about any specific third-party Guardrail version.
