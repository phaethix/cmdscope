{
  "description": "Runmark Spike S11: inject touch facts on Bash PreToolUse (additionalContext only; no deny/ask).",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"$(git rev-parse --show-toplevel)/testdata/hook-codex/runmark-codex-hook.sh\"",
            "timeout": 30,
            "statusMessage": "runmark facts"
          }
        ]
      }
    ]
  }
}
